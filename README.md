# drf-template

To create a Django project based on this template:
```bash
django-admin startproject --template https://github.com/WilsonCazarre/drf-template/archive/main.zip --name=Procfile test_drf
```